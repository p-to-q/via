interface:
  display_name: "via route"
  short_description: "A Git-tree/Google Map interface for vibe coding decisions"
  default_prompt: "Use $via-route to reason about this engineering task, explain the useful paths, and add a Git-tree/Google Map interface."

policy:
  allow_implicit_invocation: true
