# via 0.3.3

via 0.3.3 documents how people should invoke via across coding-agent surfaces.

## Documentation

- Add a wake-up matrix for Codex, ChatGPT-style shortcuts, Claude Code-style slash commands, and natural-language triggers.
- Keep `$via-route` as the direct Codex Skill invocation.
- Recommend `/via` as the short command name when via is packaged into ChatGPT or Claude Code-style command surfaces.
- Clarify that `via -v` is only the CLI version check.
- Keep npm install instructions conditional until `@p-to-q/via` is published.
