# via 0.3.5

via 0.3.5 is a distribution and identity polish release.

## Interface

- Replace the SVG eyebrow with `via · a [p → q] project`.
- Add `ptoq.io` on the same baseline at the right edge.
- Keep the product wordmark lowercase everywhere.
- Remove generic route-count branding from the visible SVG surface.
- Add a warmer README introduction that explains via as a human-agent planning surface, not just an engineering artifact.

## Distribution

- Add internal notes for Codex, Claude Code, OpenCode, Aider, and unresolved OpenCloud/OpenClaw-like targets.
- Keep the README install path centered on the copy-paste prompt that an agent can read directly.
- Make npm the primary CLI install path as `@afkv/via`, with GitHub install as a fallback while the `@p-to-q` npm organization is prepared.
- Include core docs in the npm tarball while excluding repository governance, CI, evals, release-history docs, and issue templates.
