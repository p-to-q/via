# via 0.3.9

via 0.3.9 gives the product a clearer public description.

## Product language

README hero:

> See the paths before you build.

> via helps your coding agent reason through the options, then maps 3 potential engineering paths.

GitHub, npm, Plugin, and Skill description:

> A Git-tree/Google Map interface for vibe coding decisions.

The hero states the user moment and the supporting line explains the result. Short directory and package copy defines the product directly.

Visible organization copy is now `[p → q]`. Machine identifiers remain unchanged where required, including `github.com/p-to-q`, `p-to-q/via`, npm scopes, schema URLs, and `ptoq.io`.

## Package

```bash
npm install --global @afkv/via@0.3.9
```
