# via 0.3.4

via 0.3.4 corrects the install and wake-up language.

## Documentation

- Treat `skills/via-route/SKILL.md` as the Skill entrypoint that loads the RouteSpec contract, JSON Schema, evaluator, and renderer links.
- Provide a copy-paste prompt that tells the user's coding agent to read the GitHub `SKILL.md` file and follow it to install and configure Via.
- Document `/via` as the explicit chat-box wake-up phrase, not a future packaging suggestion.
- Keep `$via-route` as the direct Codex Skill invocation.
- Keep natural language as an implicit trigger for advanced agents.
- Keep `via -v` as a CLI version check only.
